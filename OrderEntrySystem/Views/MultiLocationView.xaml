<UserControl x:Class="OrderEntrySystem.MultiLocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderEntrySystem"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="SharedResources.xaml" />
    </UserControl.Resources>
    <Grid>
        <DockPanel Margin="-29,0,0,0">
            <Grid DockPanel.Dock="Bottom">
                <local:SelectedItemsView />
            </Grid>

            <ListView ItemsSource="{Binding Path=AllLocations}" ItemContainerStyle="{StaticResource ResourceKey=ListViewStyle}" />

        </DockPanel>
    </Grid>
</UserControl>
